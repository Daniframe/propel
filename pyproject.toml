[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "propensity"
version = "0.1.0"
description = "PROPEL, the PROPensity Estimation Library: demand-interval annotation and propensity fitting for AI models"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Daniframe" }]
requires-python = ">=3.10"
keywords = ["ai evaluation", "propensity", "psychometrics", "item response theory", "llm", "annotation"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "numpy>=1.24",
    "scipy>=1.10",
    "pandas>=2.0",
    "statsmodels>=0.14",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
# One extra per provider SDK: installing one is never required to use another.
openai = ["openai>=1.40"]
azure = ["openai>=1.40"]
anthropic = ["anthropic"]
google = ["google-genai"]
http = ["httpx"]
plot = ["matplotlib>=3.5", "seaborn>=0.12"]
dotenv = ["python-dotenv"]
all = ["propensity[openai,anthropic,google,http,plot,dotenv]"]
dev = ["pytest"]

[project.scripts]
propel-annotate = "propensity.cli.annotate:main"
propel-fit = "propensity.cli.fit:main"

[project.urls]
Paper = "https://arxiv.org/abs/2602.18182"

[tool.setuptools.packages.find]
include = ["propensity*"]

[tool.setuptools.package-data]
# The rubrics ship with the package: the catalogue, the shared presentation block, every version.
propensity = ["rubrics/*.md", "rubrics/*.yaml", "rubrics/*/*.md"]

[tool.pytest.ini_options]
testpaths = ["tests"]
